---
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <meta name="description" content="Is Saga back from hiatus?">

  {% style %}
    * {
      box-sizing: border-box;
    }

    html, body {
      padding: 0;
      margin: 0;
      font-family: -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica Neue,Arial,Noto Sans,sans-serif,Apple Color Emoji,Segoe UI Emoji,Segoe UI Symbol,Noto Color Emoji;
    }

    main {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .sr-only {
      border: 0;
      clip: rect(0 0 0 0);
      clip-path: inset(50%);
      height: 1px;
      margin: -1px;
      overflow: hidden;
      padding: 0;
      position: absolute;
      width: 1px;
      white-space: nowrap;
    }

    .entry {
      text-align: center;
    }

    .entry__answer {
      margin: 0 0 .875rem 0;
      font-weight: bold;
      font-size: 5rem;
    }

    a, a:visited, a:active {
      color: #000;
      text-decoration: none;
    }

    a:hover, a:focus {
      text-decoration: underline;
    }

    .links {
      position: absolute;
      bottom: 1rem;
    }
  {% endstyle %}

  <title>Is Saga back yet?</title>
</head>
<body>
  <main>
    <h1 class="sr-only">Is Saga back yet?</h1>

    {% for entry in entries | tail(1) %}
      <div class="entry">
        <p class="entry__answer">{{ entry.answer }}</p>
        {% if entry.blurb %}
          <aside>({{ entry.blurb }})</aside>
        {% endif %}
      </div>
    {% endfor %}

    <div class="links">
      <a href="http://www.thesagashop.com/" rel="noopener">The Saga Shop</a>
    </div>
  </main>
</body>
</html>
